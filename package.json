{
  "name": "jni",
  "version": "1.0.0",
  "description": "Static website for Let's Nail It, a Bangalore murder mystery experience.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/styles.css -o ./assets/css/site.css --minify",
    "build:images": "node ./scripts/optimize-images.mjs",
    "build": "npm run build:css && npm run build:images",
    "validate": "node ./scripts/validate-site.mjs"
  },
  "private": true,
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "type": "commonjs",
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.11",
    "sharp": "^0.34.5",
    "tailwindcss": "^3.4.17"
  }
}
